-- high level cava config structure
local cava_config = {
    frequency = 1000,     -- refresh rate in sec
    bars = 150,             -- number of bars or function points
    interpolation = false,  -- function if true othervice bars
    overlap_panel = true,   -- due this option cava will overlap panel
    refresh_rate = 1000,
}

return cava_config