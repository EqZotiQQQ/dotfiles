local gears      = require("gears")
local debug      = require("dbg")
local utils      = require("lib.helpers")
local signals    = require("system.signals")

local file_utils = utils.file

local rom        = {}

function rom:init(update_time)
end

function rom:update()
end

return rom
